{
  "name": "a2ui-renderer",
  "version": "0.9.0-alpha.2",
  "private": true,
  "author": "io-q",
  "repository": {
    "type": "git",
    "url": "git@github.com:io-q/a2ui-renderer.git"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "bun run --filter @a2ui-renderer/stdlib build && bun run --filter @a2ui-renderer/react build && bun run --filter @a2ui-renderer/shadcn build && bun run --filter @a2ui-renderer/scanner build && bun run --filter demo build",
    "test": "bun run --filter '*' test",
    "dev": "bun run --filter demo dev"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}